quantidade = prompt('Quantidade de hamburguers');

queijo = quantidade * 2 * 0.05;
presunto = quantidade * 0.05;
carne = quantidade * 0.1;

resultado = 'Necessario: ' + queijo + ' kg de queijo, ' + presunto + ' kg de presunto, ' + carne + 'kg de carne';

alert(resultado);
